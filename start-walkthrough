#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PORT="${1:-8000}"
URL="http://localhost:${PORT}/world.html"

echo "Genesis Frame walkthrough start"
echo "--------------------------------"
echo "1) Open: ${URL}"
echo "2) Let autoplay run (continuity baseline)"
echo "3) Use global strip: Mode / Frame / Attention / More"
echo "4) Open Semantic frame: Narrow / Expand / More"
echo "5) Trigger interaction and watch deferred next-frame behavior"
echo "6) Export/import Aztec carrier to demonstrate verify-before-apply"
echo ""
echo "Serving atomic-kernel on port ${PORT}..."

cd "${ROOT_DIR}/atomic-kernel"
python3 -m http.server "${PORT}"
