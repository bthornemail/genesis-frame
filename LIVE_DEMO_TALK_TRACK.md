# Live Demo Talk Track

Use this during live presentation to stay concise, accurate, and proof-safe.

## 60-Second Version

1. `./start-walkthrough`
2. "The world is already running; continuity is the default."
3. "Primary control law is only Mode, Frame, Attention, More."
4. "Interaction does not rewrite the current frame; it queues lawful next-frame change."
5. "Import/export uses verified artifact carriers; invalid payloads fail closed."
6. "Witness reads and receipts; law commits."
7. "All of this is backed by executable checks: currently 140/140."

---

## 5-Minute Version

### 1) Open and establish continuity

- Run `./start-walkthrough`.
- Let autoplay run briefly.
- Say: "Users enter an ongoing canonical trace; motion does not depend on interaction."

### 2) Show primary UI law

- Point to `Mode / Frame / Attention / More`.
- Say: "Everything routes through one control grammar; advanced tools are collapsed behind More."

### 3) Show semantic attention behavior

- Open Semantic frame.
- Toggle `Narrow` and `Expand`.
- Say: "Attention changes projection density, not canonical truth."

### 4) Show bounded interaction law

- Trigger one user change.
- Say: "This opens bounded interrupt behavior and applies through deferred next-frame commit."
- If available, point to readout/receipt status.

### 5) Show carrier verification

- Export an artifact carrier (Aztec PNG).
- Re-import it.
- Say: "Carrier is transport-only; payload is verified before apply."

### 6) Close on proof-backed docs

- Reference:
  - `atomic-kernel/docs/ESCAPE_ACCESS_LAW.md`
  - `atomic-kernel/docs/WITNESS_PLANE_SPEC.md`
  - `atomic-kernel/docs/112_PROOFS_MATRIX.md`
- Say: "Claims are tied to executable lanes, not only prose."

---

## Proof-Safe Phrases

- "Deterministic replay under fixed inputs."
- "Bounded interrupt with deterministic return."
- "Deferred next-frame commit."
- "Fail-closed validation on malformed/unknown carrier input."
- "Projection is not source-of-truth."
- "Witness inspects and receipts; it does not directly mutate canonical state."
- "Branch state remains anchored to canonical reference."

---

## Phrases to Avoid

- "The AI controls the world."
- "Everything is random."
- "Any change applies immediately everywhere."
- "The renderer is the source of truth."
- "Pause time changes canonical scope."
- "This proves everything mathematically."
- "Canonical state is editable directly from UI."

---

## Optional Closing Sentence

"Genesis Frame is law-first: continuity is canonical, interaction is bounded, change is receipted, and reconciliation is replayable."
